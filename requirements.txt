# Runtime
beautifulsoup4
fastapi
curl_cffi==0.9.0
httpx
pydantic
pydantic-settings
pymongo>=4.9,<5
requests
uvicorn[standard]
# Runtime browser fallback (the Camoufox browser binary is provisioned separately
# in the MyAgent environment).
camoufox==0.5.4
playwright==1.60.0

# Test
pytest
