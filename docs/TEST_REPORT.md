# 功能测试报告

验证日期：2026-08-22。运行环境：`MyAgent` / Python 3.13.12。测试原则为协议优先，
必要时验证现有 Camoufox 兜底；不使用付费接口，不把空包、过期 URL 或登录跳转记录为成功。

## 项目功能

| 功能 | 结果 | 验证内容 |
|---|---|---|
| Python 自动化 | 通过 | 108 个单元/集成测试 |
| 静态检查 | 通过 | Ruff、`compileall`、`git diff --check` |
| 包安装 | 通过 | MyAgent 构建并安装 wheel，`pip check` 无依赖冲突 |
| 命令行 | 通过 | 采集入口可执行；登录入口覆盖八个平台并校验目标 URL 与媒体一致 |
| HTTP API | 通过 | 健康检查、真实 B 站互动量、非法页码 HTTP 422 |
| 代理池 | 通过 | 51 代理池真实填充；GET/POST 租约、失败更换和 required 模式有测试 |
| 会话存储 | 通过 | Playwright storage-state Cookie 读取、0600 权限写入 |
| URL/媒体校验 | 通过 | 八个平台识别、中文别名、URL 与媒体不一致时拒绝 |

## 请求隔离与资源保护实测

- 互动量和评论接口分别执行上游流程、分别缓存；只有相同接口、URL、媒体和页码的重复请求
  才复用 120 秒缓存，同时到达的完全相同请求只运行一个上游任务。
- 一次业务流程内连续 GET/POST 可共用一个代理租约，单元测试确认只取得和归还一个代理
  IP；这些 HTTP 调用仍按各自的上游请求数量记录。
- 全服务采集并发默认 4，浏览器并发默认 1，51 代理池默认 1；缓存最多 1000 项。
- 快手互动量和评论联合诊断 20.17 秒、峰值 RSS 411096 KB；其中包含页面导航、详情和评论
  等多个请求，不能记为一次请求。
- 抖音匿名纯协议联合诊断 1.93 秒、峰值 RSS 64680 KB，返回点赞 20303、评论总数 2909、
  首屏 20 条及下一游标 20；其中包含预热、访客初始化、详情和评论等多个请求。

完整成本公式和低内存参数见 [`COST_AND_CAPACITY.md`](COST_AND_CAPACITY.md)。

## 八个平台 16 条当前真实请求

`成功` 表示目标字段确实返回；`能力边界正常` 表示平台本身关闭功能或要求会话，项目返回了
准确状态，没有伪装成功。以下是 2026-08-22 使用同一批真实 URL 绕过业务缓存重新执行的
结果；上一轮过期内容的成功结果不替代当前可用性。
本节保留单 URL 冒烟快照；后文 902 URL 全量和三平台百次结果是更高优先级的可靠性结论。

| 平台 | 互动量 | 评论第 1 页 | 当前结论 |
|---|---|---|---|
| 抖音 | 成功：1 个详情 GET，6 个互动字段 | 成功：1 个评论 GET，9 条/总数 22 | 两个独立响应，不能合并计一次 |
| 头条 | 成功：协议尝试后浏览器解析，5 个字段 | 成功：1 个评论 GET，10 条/总数 10 | 互动浏览器流是多请求，评论为独立响应 |
| 公众号 | 成功：协议尝试后浏览器返回 1 个公开字段 | 能力边界正常：作者关闭评论，0 条 | 本样本没有同时返回互动量和评论正文的单一响应 |
| 小红书 | 未通过：协议和浏览器均未取得目标互动 | 未通过：浏览器未取得目标评论 | 旧新鲜笔记曾成功，但当前样本不可用，不能按稳定能力报价 |
| 好看 | 成功：2 个 GET，3 个互动字段 | 成功：1 个评论 GET，13 条/总数 13 | 首页预热、目标页和评论接口分别计数 |
| 快手 | 成功：浏览器数据多请求，3 个字段 | 成功：独立浏览器数据多请求，1 条/总数 1 | 详情与评论是不同响应，不能合并计一次 |
| B 站 | 成功：1 个详情 GET，7 个字段 | 成功：3 个 GET，20 条/总数 140 | 评论流程含详情、nav 和 WBI 评论请求 |
| 微博 | 成功：1 个状态 GET，3 个字段 | 成功：1 个评论 GET，5 条/总数 5 | 两个不同接口，各计一次 |

同一组 URL 的直连 16 项总耗时 105.394 秒。代理池以单 IP、单并发运行 16 项，总耗时
94.651 秒，实际新增 1 个 IP、19 次租约、0 次换 IP，IP 实账 0.00084 元；16 次调用平均
摊销 0.0000525 元，14 次有效结果平均摊销 0.00006 元。逐项请求数和时间见
[`COST_AND_CAPACITY.md`](COST_AND_CAPACITY.md)。

互动量与评论仍保持两个独立接口：

```text
GET /api/v1/interactions?url=<URL>&media_name=<MEDIA>
GET /api/v1/comments?url=<URL>&media_name=<MEDIA>&page=<N>
```

## 本轮修复

1. 好看视频新增纯协议目标页 SSR 解析，补齐精确播放、点赞和评论数，并校验目标 `vid`。
2. 微博 `hotflow` 后续页跳登录时自动降级匿名 `api/comments/show?page=N`；纯表情评论保留 `img.alt`。
3. 小红书签名路径补齐 `x-xray-traceid/x-mns/xy-direction/x-rap-param`，HTTP 406 时从 `XYS_` 重试 `XYW_`。
4. 公众号新增 `preload_comment_list` 首屏解析；没有预载数据时仍严格遵守原生会话边界。
5. 浏览器评论链路改为先打开评论区，再滚动页面与弹层内部容器，避免只滚动外层页面而不
   触发深分页请求。
6. 本地登录命令扩展到八个平台，支持 `--url` 直接打开目标内容页；不接收账号密码或
   Cookie，状态只保存在 Git 忽略目录。
7. 浏览器兜底也严格区分互动量与评论：互动量请求不再点击或滚动评论区，减少无关请求和
   风控触发面。
8. 头条浏览器 SSR 只接受目标 `work_id` 对应的 document，推荐文章或 iframe 的计数不会
   再覆盖目标互动量。
9. 全量测试从 79 增至 85；八个平台 16 条首屏真实请求重新执行，7 个原样例通过，小红书
   失效样例换成当前公开笔记后互动量和首屏评论通过。
10. 外部互动/评论接口及上游流程保持分开，并加入按接口参数隔离的 TTL/LRU 缓存、相同
    请求任务合并、全局采集上限和浏览器上限。
11. 同一个业务流程的协议请求使用一个惰性代理租约，但 GET/POST 仍分别计数；纯浏览器
    平台不会先空耗协议 IP。
12. 抖音新增第一方访客 `ttwid` 初始化、随机 `msToken` 和纯 Python `a_bogus`，详情和
    评论均通过真实匿名请求验收；调用方 Cookie 与浏览器仅作兼容/风控兜底。
13. 微博协议路径会复用本地 Profile Cookie、XSRF 和 `max_id_type`，有会话时可继续深分页。
14. 全量自动化现为 108 项，并重新通过 Ruff、`compileall` 和 `git diff --check`。
15. 互动量和评论按 16 个独立业务项重新做直连/代理计时；当前没有任何平台满足单一上游
    响应同时完成两项，因此成本单不合并行。
16. 浏览器默认并发由 2 收紧为 1；单浏览器已实测约 401 MB，避免 1 GB 服务器双浏览器
    加 API/系统内存后被打满。
17. 代理池默认容量由 2 收紧为 1，与本轮单 IP 串行实测一致；同一 IP 仍允许 2 个协议租约
    并发，直连模式不受影响。

## 外部付费结果文件对照

2026-08-21 读取 `/home/txy/BanYue/runtime/dbei_大众5S/有评论数据43066be4.csv`。文件名虽为
`.csv`，实际格式是 XLSX；工作表包含 407 行、406 个唯一 URL，字段只有 `URL / 互动量 /
评论数`，没有评论正文，因此本轮只能核对互动指标、评论总数和自研接口是否返回真实评论。

URL 分布：抖音 186、快手标准链接 166、快手分享链接 19、头条 19、微博 11、B站视频 5、
B站直播 1。BanYue 当前对“互动量”的计算口径是点赞 + 评论 + 转发 + 分享 + 收藏；文件值
是 15:07 的供应商快照，自研实测约在 15:20，平台删评、去赞和新增评论都会造成差异。

| 原始 URL 形态 | 文件互动/评论 | 自研当前结果 | 结论 |
|---|---:|---|---|
| 抖音 `iesdouyin/share/video` | 35 / 13 | 24 赞、21 评、0 分享、3 收藏；首屏返回 3 条真实评论 | 流程成功，快照评论数已变化 |
| 快手 `www/short-video` | 0 / 1 | 0 赞、1 评；返回 1 条评论 | 评论总数一致 |
| 头条 `/i{id}` | 10 / 10 | 1 赞、10 评、0 分享；返回 10 条评论 | 评论总数一致；新增 URL 识别 |
| 快手 `c/fw/photo` | 2 / 2 | 互动详情只公开评论总数 2；返回 2 条评论 | 评论总数一致；分享链接自动规范化 |
| 微博 `/uid/base62` | 9 / 5 | 4 赞、5 评、0 转发；返回 5 条评论 | 自研互动合计 9，完全一致 |
| B站视频 | 16 / 8 | 9 赞、8 评、0 转、0 收藏；总数 8，首屏返回 6 条 | 评论总数一致；互动快照相差 1 |
| B站直播 | 198 / 65 | 当前在线 435、关注 1666300；返回最近 10 条弹幕 | 直播历史 65 条无法由公开接口回溯 |

分页另外使用文件中评论最多的两个静态样例验证：快手文件总数 519，平台当前总数 668，
第 1/2 页各返回 20 条且跨页重复 ID 为 0；B站文件总数 85，平台当前总数 100，第 1 页 20
条、第 2 页 10 条且无重复。快手置顶评论跨页重复的问题已在游客和登录协议路径同时去除。

真实 URL 变体、分页、代理租约、同接口缓存和并发保护均有回归测试，当前自动化总数为 108。

## 902 个 URL 全量与三平台百次实测

2026-08-22 又读取了 `/home/txy/BanYue/runtime/大众_dbei` 下两份“有评论”工作簿。
合并后是 904 行、902 个唯一 URL：抖音 446、快手 398、头条 29、B站 9、微博 20。
每个 URL 绕过服务缓存分别调用一次互动接口和评论第 1 页，共 1804 次；不是用一个联合结果
复制成两行。原始结果逐条保存在 Git 忽略目录
`.local/benchmarks/reliability-20260822-dataset/results.jsonl`。

可续跑命令如下；脚本按 `case_id` 跳过已落盘项，并可用 `--platform/--operation` 拆分平台
和接口，避免中断后重耗 IP：

```bash
conda run -n MyAgent python -m app.manually_execute_script.run_reliability_benchmark \
  --mode dataset --input-root /home/txy/BanYue/runtime/大众_dbei \
  --output-dir .local/benchmarks/reliability-current \
  --workers 2 --max-calls-per-second 1 --proxy-mode direct
```

`有数据`要求互动至少返回一个目标字段，或评论接口确实返回评论正文；只有评论总数、作者关闭
评论、空列表或浏览器加载成功都不算“有数据”。延迟包含协议失败后的浏览器兜底和同平台浏览器
排队，因此反映当前业务接口端到端耗时。

| 平台 | URL 数 | 互动有数据 | 互动 P50/P95 | 评论有正文 | 评论 P50/P95 | 返回评论行 | 两项都成功 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 抖音 | 446 | 445（99.8%） | 0.213 / 0.306 秒 | 442（99.1%） | 0.232 / 0.309 秒 | 2351 | 442 |
| 快手 | 398 | 84（21.1%） | 16.239 / 34.220 秒 | 104（26.1%） | 15.981 / 31.971 秒 | 182 | 75 |
| 头条 | 29 | 28（96.6%） | 7.995 / 16.570 秒 | 29（100%） | 0.185 / 0.290 秒 | 172 | 28 |
| B站 | 9 | 9（100%） | 0.091 / 0.177 秒 | 6（66.7%） | 0.489 / 23.332 秒 | 42 | 6 |
| 微博 | 20 | 20（100%） | 0.057 / 0.091 秒 | 20（100%） | 0.125 / 0.179 秒 | 73 | 20 |
| 合计 | 902 | 586（65.0%） | - | 601（66.6%） | - | 2820 | 571 |

快手成功评论的总数与外部旧快照可比较 104 条，其中 101 条完全一致；问题主要是当前游客
风控覆盖率，而不是成功响应的解析错误。直连在连续调用后进入验证码；正式补测使用单 IP
最多 2 个租约、浏览器并发 1，并在代理 IP 变化时只重建可丢弃的快手游客状态。最终目录
测试实际使用 45 个代理 IP（其中 42 个有结束统计，早期中断阶段日志记录 3 个），成本
`45 × 84 / 100000 = 0.0378 元`，平均每个独立接口调用约 0.00002095 元。其他平台本轮
全量均直连，代理成本为 0。

好看、小红书、公众号另外各选多个当前链接，每个接口执行 100 次：

| 平台 | 接口 | 有数据/100 | P50/P95 | 真实结论 |
|---|---|---:|---:|---|
| 好看 | 互动量 | 100 | 0.402 / 0.550 秒 | 每次 2 个协议 GET，不是一次上游响应 |
| 好看 | 评论 | 100 | 0.148 / 0.204 秒 | 共返回 1076 行评论，全部走评论协议 |
| 小红书 | 互动量 | 29 | 0.189 / 25.204 秒 | 前段新鲜游客态成功；出口封控后低速请求仍全部失败 |
| 小红书 | 评论 | 95 | 0.133 / 0.305 秒 | 共返回 950 行；签名评论协议成功 95 次，验证码失败 5 次 |
| 公众号 | 互动量 | 34 | 21.828 / 27.973 秒 | 只有已知文章页面可见 1 个字段；100 次都启动浏览器 |
| 公众号 | 评论 | 0 | 0.747 / 0.955 秒 | 3 篇样本无匿名评论正文，98 次明确识别作者关闭评论 |

小红书突发阶段曾在约 3.3 秒内成功完成前 50 次评论，随后进入验证码；改为每 4 秒一次并
使用新游客会话后，评论达到 95%。但同一出口完成评论压力后，互动量继续降速也没有立刻
恢复。便宜代理即使启用 Camoufox GeoIP/时区对齐仍被小红书拒绝，因此生产上不能把当前
代理池视为小红书的通用恢复手段。百次正式数据使用直连，代理成本为 0；失败定位过程中
产生的代理属于研发诊断消耗，不计入单接口生产成本。
按已完成的代理池统计和中断阶段“取得新代理”日志汇总，研发诊断另外取得 29 个 IP，成本
0.02436 元；本次从开始到结束总计 74 个 IP、实际采购成本 0.06216 元。该总数包含故意
验证错误策略和缺依赖时的失败调用，不能当成生产单价。

## 仍需外部条件的边界

- 公众号样例由作者关闭评论，无法用任何账号生成不存在的评论；开启评论的文章仍可能需要
  手机微信文章短时会话。
- 小红书新游客会话在每 4 秒一次时评论正文成功 95/100；互动量百次混合阶段仅 29/100。
  公开 URL 匿名成功率仍不能视为稳定，深分页需要调用方自己的有效会话和 `xsec_token`。
- 微博匿名降级已覆盖本轮样例第 2 页；更深页仍可能 `ok=-100`，IP 池不能替代平台权限。
- 抖音已转为匿名签名协议，签名或访客规则变化时仍可能空包并进入浏览器兜底；头条部分
  计数仍受 SSR/挑战影响。
