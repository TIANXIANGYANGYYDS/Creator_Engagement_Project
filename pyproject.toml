[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "creator-engagement"
version = "0.1.0"
description = "Protocol-first engagement and public comment collection by URL"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4",
    "curl-cffi==0.15.0",
    "fastapi",
    "httpx",
    "openpyxl>=3.1",
    "pydantic>=2",
    "pydantic-settings",
    "pymongo>=4.9,<5",
    "requests",
    "uvicorn[standard]",
    "xhshow==0.2.0",
]

[project.optional-dependencies]
test = ["pytest"]
browser = ["camoufox==0.5.4", "maxminddb>=2.6,<3", "playwright==1.60.0"]
wechat = ["mitmproxy==12.2.3"]

[project.scripts]
creator-engagement = "app.manually_execute_script.fetch_url_engagement:main"
creator-engagement-login = "app.manually_execute_script.login_platform:main"
creator-engagement-wechat-bridge = "app.manually_execute_script.wechat_session_bridge:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
